{
  "name": "utils",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "scripts": {
    "lint": "eslint \"src/**/*.ts*\" && tsc --noEmit",
    "lint:fix": "eslint --fix \"src/**/*.ts*\" && tsc --noEmit",
    "test": "jest",
    "test:i": "jest --runInBand",
    "test:coverage": "jest test --coverage",
    "test:w": "jest --watch"
  },
  "devDependencies": {
    "@types/react": "^18.0.32",
    "@types/react-dom": "^18.0.11",
    "eslint": "^8.37.0",
    "eslint-config-custom": "*",
    "react": "^18.2.0",
    "ts-jest": "^29.1.1",
    "tsconfig": "*",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "clsx": "^2.0.0",
    "tailwind-merge": "^2.0.0"
  }
}
