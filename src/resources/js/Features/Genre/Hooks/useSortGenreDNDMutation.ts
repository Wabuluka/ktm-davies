import { useMutation } from 'react-query';
import axios from 'axios';
import { Genre } from '../Types';
import { useToast } from '@chakra-ui/react';


export const useSortGenreDNDMutation = () => {
  const toast = useToast();
  
  return {}
}
